# Copie este arquivo para ".env" ao rodar localmente.
# No Railway, DATABASE_URL é preenchida automaticamente quando você
# adiciona um serviço PostgreSQL ao projeto — não precisa copiar nada lá.

DATABASE_URL=postgresql://usuario:senha@localhost:5432/cofre
JWT_SECRET=troque-isto-por-um-texto-aleatorio-longo
PORT=3000
